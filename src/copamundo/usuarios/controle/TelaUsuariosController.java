package copamundo.usuarios.controle;

import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Node;
import javafx.scene.layout.StackPane;

/**
 * Controller de TelaUsuarios.fxml.
 * Gerencia navegação interna entre Usuários, Relatórios e Configurações.
 */
public class TelaUsuariosController {

    @FXML
    private void handleMenuUsuarios() {
        // já está na tela de usuários — sem ação necessária
    }

    @FXML
    private void handleMenuRelatorios() {
        // navegação para relatórios tratada pela TelaPrincipal
    }
}
