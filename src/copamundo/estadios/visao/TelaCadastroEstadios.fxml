<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox spacing="15" stylesheets="@estilo.css"
      xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="copamundo.estadios.visao.TelaCadastroEstadiosController">

    <padding>
        <Insets bottom="15" left="15" right="15" top="15"/>
    </padding>

    <Label text="Cadastro de Estadios" styleClass="label-titulo"/>

    <GridPane hgap="10" vgap="10">
        <Label text="Nome:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
        <TextField fx:id="txtNome" promptText="Nome do estadio"
                   GridPane.rowIndex="0" GridPane.columnIndex="1"/>

        <Label text="Cidade:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
        <TextField fx:id="txtCidade" promptText="Cidade"
                   GridPane.rowIndex="1" GridPane.columnIndex="1"/>

        <Label text="Capacidade:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
        <TextField fx:id="txtCapacidade" promptText="Capacidade"
                   GridPane.rowIndex="2" GridPane.columnIndex="1"/>
    </GridPane>

    <HBox spacing="10" alignment="CENTER">
        <Button text="Novo" onAction="#limparCampos"/>
        <Button text="Salvar" onAction="#salvarEstadio"/>
        <Button text="Limpar" onAction="#limparCampos"/>
    </HBox>

    <TableView fx:id="tabelaEstadios" prefHeight="220">
        <columns>
            <TableColumn fx:id="colunaNome" text="Nome" prefWidth="180"/>
            <TableColumn fx:id="colunaCidade" text="Cidade" prefWidth="160"/>
            <TableColumn fx:id="colunaCapacidade" text="Capacidade" prefWidth="120"/>
        </columns>
    </TableView>

</VBox>
